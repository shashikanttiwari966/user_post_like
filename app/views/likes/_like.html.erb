<% if post.likes.count == 1 %>
      <%= post.likes.count %>
      <%= link_to "Unlike", like_path(post_id:post.id, like_post_id: post.likes.first.id), method: :delete, remote: true %>
<% else %>
      <%= link_to "Like", likes_path(post_id:post.id), method: :post, remote:true %>
<% end %>
    