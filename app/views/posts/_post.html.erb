 
<div class="divTableCell"><%= post.title %></div>
<div class="divTableCell"><%= post.description %></div>
<div class="divTableCell">
 <%= link_to 'Edit', edit_post_path(post), remote: true, class:'btn btn-success text-light' %>
 <%= link_to 'Destroy', post_path(post.id), method: :delete, class:'btn btn-danger text-light', data: { confirm: 'Are you sure?' } %>
</div>